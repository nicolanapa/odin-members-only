<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Members Only</title>
    </head>
    <body>
        <% if (locals.user) { %>
        <h1>
            Hi,
            <%= user.email %>
        </h1>
        <% } %>
    </body>
</html>
